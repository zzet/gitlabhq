- team = Team.find_by(id: event.data['team_id'])
- group = Group.find_by(id: event.data['group_id'])

deleted
= link_to_or_deleted(team, 'team')
from
= link_to_or_deleted(group, 'group')
by
= link_to event.author.name, user_path(event.author.username)
