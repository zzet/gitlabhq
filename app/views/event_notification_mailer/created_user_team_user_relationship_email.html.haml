= raw "#{ link_to @member.name, user_url(@member.username) } was added to '#{ @team.name }' team by #{ link_to @user.name, user_url(@user.username) } with '#{ @default_access }' default access in projects."
