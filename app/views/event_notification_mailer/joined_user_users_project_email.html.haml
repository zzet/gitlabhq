= render "hello", user: @user
= "User #{link_to @member.name, user_url(@member.username)} was added to project members of #{link_to @project.name_with_namespace, project_url(@project.name_with_namespace)} project with #{@up.project_access} permission by #{link_to @user.name, user_url(@user.username)}"
= render "footer", user: @user
