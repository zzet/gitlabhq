= raw "Permissions for user #{ link_to @member.name, user_url(@member.username) } in project #{ link_to @project.path_with_namespace, project_url(@project.path_with_namespace) } was updated by #{ link_to @member.name, user_url(@member.username)} [updated]"
%br
%table{style: "background: #eee; color: #444"}
  %tr
    %th Attribute
    %th New value
    %th Old value
  - @changes.each do |key, values|
    %tr
      %td= key
      %td= values.last
      %td= values.first
%br

