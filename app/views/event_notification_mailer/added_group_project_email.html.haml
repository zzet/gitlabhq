= render "hello", user: @user
= raw "Project #{ link_to @project.path_with_namespace, project_url(@project.path_with_namespace) } was added to #{ link_to @group.name, group_url(@group.path) } group of projects by #{ link_to @user.name, user_url(@user.username) }."
= render "footer", user: @user
