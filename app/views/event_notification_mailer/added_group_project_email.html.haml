= render "hello", user: @user
= "Project #{ link_to @project.name_with_namespace, project_url(@project.name_with_namespace) } was added to #{ link_to @group.name, group_url(@group.path) } by #{ link_to @user.name, user_url(@user.username) }"
= render "footer", user: @user
