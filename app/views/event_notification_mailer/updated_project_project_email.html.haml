= render "hello", user: @user
= raw "Project #{ link_to @project.path_with_namespace, project_url(@project.path_with_namespace) } was updated by #{ link_to @user.name, user_url(@user.username)}"
= render "footer", user: @user
