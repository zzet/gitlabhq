= raw "#{ link_to @member.name, user_url(@member.username) } was added to '#{ link_to @project.name_with_namespace, project_url(@project.path_with_namespace) }' project as '#{ @project_access }' by #{ link_to @user.name, user_url(@user.username) }."
