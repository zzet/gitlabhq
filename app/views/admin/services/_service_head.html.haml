%h3.page-title
  - if @service.active?
    %span.cgreen
      %i.icon-circle
  - else
    %span.cgray
      %i.icon-circle-blank
  = @service.title
  service

%p= @service.description

.back-link
  = link_to admin_services_path do
    &larr; to services

%hr
