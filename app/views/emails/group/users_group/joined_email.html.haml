= raw "#{ link_to @member.name, user_url(@member.username) } was added to #{ link_to @group.path, group_url(@group.path)} group with #{@group.users_groups.where(user_id: @member).first.human_access} permission by #{link_to @user.name, user_url(@user.username)}"
