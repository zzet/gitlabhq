= raw "#{ link_to @user.name, user_url(@user.username) } removed #{@members.count} members from #{ link_to @project.path_with_namespace, project_url(@project.path_with_namespace) } project team:"
%br
%table
  %caption Removed members
  %tr
    %th Name
  - @members.each do |pm|
    %tr
      %td= link_to pm.name, user_url(pm.username)
%br

%table
  %caption Project team members now
  %tr
    %th Name
    %td Project access
  - @project.users_projects.each do |pm|
    %tr
      %td= link_to pm.user.name, user_url(pm.user.username)
      %td= pm.human_access
%br
