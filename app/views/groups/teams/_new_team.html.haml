= form_for @team_group_relation, url: group_teams_path(@group), html: { class: 'form-horizontal users-group-form' } do |f|
  %fieldset
    %legend
      Assign teams on
      %strong #{@group.name}
      group

    %p 1. Choose teams you want assigned to the group
    .form-group
      = f.label :team_ids, "Teams", class: 'control-label'
      .col-sm-10= teams_select_tag(:team_ids, multiple: true, class: 'input-large')

    .form-actions
      = f.submit 'Assign group on selected teams', class: "btn btn-create"
