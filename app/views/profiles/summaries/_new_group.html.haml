= form_for relation, url: profile_summary_groups_path(summary), html: { class: 'form-horizontal'} do |f|
  %p 1. Choose groups you want add to the summary
  .form-group
    = f.label :group_ids, "Groups", class: 'control-label'
    .col-sm-10= groups_select_tag(:group_ids, multiple: true, class: 'input-large input-clamp')

  .form-actions
    = f.submit 'Add selected groups', class: "btn btn-create"
