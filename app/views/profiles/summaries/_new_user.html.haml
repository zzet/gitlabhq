= form_for relation, url: profile_summary_users_path(summary), html: { class: 'form-horizontal'} do |f|
  %p 1. Choose users you want add to the summary
  .form-group
    = f.label :user_ids, "Users", class: 'control-label'
    .col-sm-10= users_select_tag(:user_ids, multiple: true, class: 'input-large input-clamp')

  .form-actions
    = f.submit 'Add selected users', class: "btn btn-create"
