- tree, commit = submodule_links(submodule_item)
%tr{ class: "tree-item" }
  %td.tree-item-file-name
    %i.icon-archive
    %span
      = link_to_submodule(submodule_item)
    @
    %span.monospace
      - if commit.nil?
        #{submodule_item.id[0..10]}
      - else
        = link_to "#{submodule_item.id[0..10]}", commit
  %td
  %td.hidden-xs
