%h3.page_title
  Public project files
  %small share file content with others without authentication

  .pull-right
    = link_to profile_tokens_path(current_user), class: "btn btn-small grouped"  do
      My public files

%hr
.row
  .span12
    = render 'tokens'

