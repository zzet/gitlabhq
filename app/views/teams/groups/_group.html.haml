%li
  = link_to group.name, group
  .pull-right
    = link_to 'Members', members_group_path(group), id: "edit_#{dom_id(group)}", class: "btn btn-small"
    = link_to 'Remove', team_group_path(@team, group), confirm: remove_group_from_team_message(@team, group), method: :delete, class: "btn btn-small btn-remove"
